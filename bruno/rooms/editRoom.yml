info:
  name: editRoom
  type: http
  seq: 4

http:
  method: PUT
  url: '{{baseUrl}}/rooms/{{roomId}}'
  body:
    type: json
    data: |2-
        {
          "name": "Sala para excluir",
          "capacity": 8,
          "location": "10º andar",
          "description": "Sala teste para excluir",
          "amenities": [
            "projector",
            "whiteboard",
            "tv"
          ]
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
