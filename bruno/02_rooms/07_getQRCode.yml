info:
  name: getQRCode
  type: http
  seq: 7

http:
  method: GET
  url: '{{baseUrl}}/rooms/{{roomId}}/QRCode'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
