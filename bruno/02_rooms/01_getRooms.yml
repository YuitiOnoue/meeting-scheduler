info:
  name: getRooms
  type: http
  seq: 1


http:
  method: GET
  url: '{{baseUrl}}/rooms'
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("roomId", res.body[0].id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
