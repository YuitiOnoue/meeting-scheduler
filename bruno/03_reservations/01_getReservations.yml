info:
  name: getReservations
  type: http
  seq: 1

http:
  method: GET
  url: '{{baseUrl}}/reservations?roomId={{roomId}}&date={{date}}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
