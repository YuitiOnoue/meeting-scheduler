info:
  name: deleteReservation
  type: http
  seq: 3

http:
  method: DELETE
  url: '{{baseUrl}}/reservations/{{reservationId}}'
  auth:
    type: bearer
    bearer:
      token: '{{token}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
