info:
  name: getRoom
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/rooms/{{roomId}}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
